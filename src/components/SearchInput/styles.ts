import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    containerInput: {
        backgroundColor: "#FFF",
        flexDirection: "row",
        width: "90%",
        borderRadius: 30,
        alignItems: "center",
        paddingHorizontal: 15,
        height: 40,
    },
    input: {
        color: "#000",
        paddingHorizontal: 15,
        width: "100%"
    }
});