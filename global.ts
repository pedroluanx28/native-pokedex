import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    verticalLine: {
        borderLeftColor: "#ccc",
        borderLeftWidth: 2,
        borderStyle: "solid",
    }
});